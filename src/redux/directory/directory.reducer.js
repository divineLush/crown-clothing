import sections from './directory.initial.js';

const INITIAL_STATE = { sections };

const directory = (state = INITIAL_STATE, { type }) => {
  switch (type) {
    default:
      return state;
  }
};

export default directory;
